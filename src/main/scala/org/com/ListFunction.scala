package org.com

object ListFunction {

  def main(args: Array[String]): Unit = {

    var str = "DimSite|Wnd.DimSite,FactSite|WnD.FactSite"
    var ListStr = str.split(",")
//    var ListMapStr = ListStr.map(f => f.split("|")[0] )


  }

}
